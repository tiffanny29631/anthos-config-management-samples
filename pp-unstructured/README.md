# Unstructured namespace repo content

This directory contains unstructured manifests intended for namespace `RepoSync`.

- `team-a/` resources are synced by the nested RepoSync in `pp-hierarchy`.
- `team-b/` resources are provided as additional unstructured sample content.
